import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'flappy-bird-clon',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
